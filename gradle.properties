# Cap memory cua Gradle daemon tren may it RAM (8GB) de tranh "CreateProcess error=8".
# Daemon chi orchestrate build; test chay o JVM fork rieng (maxHeapSize trong build.gradle).
org.gradle.jvmargs=-Xmx512m -XX:MaxMetaspaceSize=256m
org.gradle.workers.max=1
